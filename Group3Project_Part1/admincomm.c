#include "commands.h"

void addNewUser(User *allUsers, char *fileLocation)
{
    printf("Please enter a unique user name, do not include commas and keep name shorter than %d characters:\n", MAX_USERNAME);
    char username[MAX_USERNAME];
    
}